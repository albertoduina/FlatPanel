package PluginFlatPanel;import ij.IJ;import ij.ImagePlus;import ij.io.OpenDialog;import ij.plugin.PlugIn;public class SelezioneMacchina implements PlugIn {	public void run(String arg) {		IJ.log("---------------------------------");		IJ.log(ReadData.qui() + "START>");		String[] aux5 = arg.split(";");		for (String myArg : aux5)			IJ.log("arg= " + myArg);		IJ.log("---------------------------------");				long[] datiMacchina = ReadData.selectFlatPanel();		int width = (int)datiMacchina[0];		int height = (int)datiMacchina[1];		long offset = datiMacchina[2];		int dimPixel = (int)datiMacchina[3];		OpenDialog od = new OpenDialog("Choose a file", null);		String folder = od.getDirectory();		String file = od.getFileName();		String path = folder + file;		ReadData.waitHere(path);		ImagePlus imp1 = ReadData.openRaw(path, width, height, offset);								// ========= LETTURA DEL FILE FlatConfig.csv		// uso un ArrayList per poter avere una lunghezza variabile//		//		//		//		// ============================================================================										IJ.log(ReadData.qui() + "END>");		IJ.log("---------------------------------");		IJ.showMessage("Fine lavoro");	}}